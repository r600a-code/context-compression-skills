# context-compression

Public Hermes skill for preserving continuity across context compression.

What it covers:
- structured summary design
- anchored iterative compression
- post-compression recovery rules
- protection against stale TODO / stale summary takeover

Files:
- `SKILL.md` — main skill
- `skill.json` — install metadata

Planned install style:
- `npx skills add <owner>/<repo>`
- or copy this folder into your local skills directory

After pushing to GitHub, replace the placeholder URLs in `skill.json` with the final repository and raw file URLs.
